<%= form_for([@project,@project.sample_sets.build]) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="form">
    <%= f.text_field :name, :placeholder => "Name" %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
