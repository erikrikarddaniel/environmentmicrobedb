<%= form_for(@sample_set) do |f| %>
  <% if @sample_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sample_set.errors.count, "error") %> prohibited this sample_set from being saved:</h2>

      <ul>
      <% @sample_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
