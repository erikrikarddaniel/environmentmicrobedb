<%= form_for(@subject_property) do |f| %>
  <% if @subject_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subject_property.errors.count, "error") %> prohibited this subject_property from being saved:</h2>

      <ul>
      <% @subject_property.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
